#!/usr/bin/env bash

main() {
  while true; do
    read -r answer
    if [ "$answer" = 'yes' ]; then
      exit 0
    elif [ "$answer" = 'no' ]; then
      exit 1
    else
      echo 'Please enter yes or no'
    fi
  done
}

main "$@"
