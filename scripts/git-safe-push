#!/usr/bin/env bash

set -e

main() {
  git branch --set-upstream-to "origin/$(git current-branch)" > /dev/null 2>&1 &&
    git push --dry-run &&
    echo &&
    git outgoing &&
    echo &&
    echo "Do you want to push?" &&
    confirm &&
    git push
}

main "$@"
